# URL pública da API (json-server) publicada no Render — repositório "banco-json".
# Ajuste para a URL real do seu serviço no Render antes do deploy em produção.
VUE_APP_API_BASE_URL=https://banco-json-la-forneria.onrender.com
